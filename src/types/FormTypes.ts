export type FormValues = {
  data: {
    username: string;
    email: string;
    password: string;
    phone: string
  }
  };

  export type FormGuestValues = {
    data: {
    username: string;
    email: string;
    password: string;
    phone: string
    }
  }