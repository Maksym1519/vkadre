export type FormValues = {
    username: string;
    email: string;
    password: string;
    phone: number
  };

  export type FormGuestValues = {
    data: {
    username: string;
    email: string;
    password: string;
    phone: number
    }
  }