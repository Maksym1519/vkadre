export type OrderPhotoData = {
    data: {
        username: string,
        email: string,
        phone: number,
        type: string,
        date: string,
        time: string,
        length: string,
        text: string
    }
}